.idea/
.DS_Store
app/build/
app/web/dist/
app/web/node_modules/
app/web/*.tsbuildinfo
# Real configs contain credentials; only config.example.yaml is tracked
config.yaml
config.test.yaml
# HAP pairing state (persisted at runtime)
hap/
hap-test/
*.hap
# ...but the vendored brutella/hap fork is source and must be tracked.
!app/third_party/hap/
