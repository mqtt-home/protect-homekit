project_name: protect-homekit
version: 2

before:
  hooks:
    - go mod tidy
    - sh -c "cd web && npm install -g pnpm@11.5.1 && pnpm install --frozen-lockfile && pnpm run build"

builds:
  - id: protect-homekit
    main: ./main.go
    binary: protect-homekit
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mqtt-home/protect-homekit/version.Version={{.Version}} -X github.com/mqtt-home/protect-homekit/version.GitCommit={{.Commit}} -X github.com/mqtt-home/protect-homekit/version.BuildTime={{.Date}}

dockers:
  - image_templates:
      - "pharndt/protect-homekit:{{ .Tag }}-amd64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    goos: linux
    goarch: amd64
    build_flag_templates:
      - "--platform=linux/amd64"
    extra_files:
      - web/dist/
    skip_push: false

  - image_templates:
      - "pharndt/protect-homekit:{{ .Tag }}-arm64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    goos: linux
    goarch: arm64
    build_flag_templates:
      - "--platform=linux/arm64"
    extra_files:
      - web/dist/
    skip_push: false

docker_manifests:
  - id: tag
    name_template: "pharndt/protect-homekit:{{ .Tag }}"
    image_templates:
      - pharndt/protect-homekit:{{ .Tag }}-amd64
      - pharndt/protect-homekit:{{ .Tag }}-arm64

  - id: latest
    name_template: "pharndt/protect-homekit:latest"
    image_templates:
      - pharndt/protect-homekit:{{ .Tag }}-amd64
      - pharndt/protect-homekit:{{ .Tag }}-arm64

release:
  disable: false
  mode: replace
  draft: false
  prerelease: auto
  make_latest: true
  skip_upload: true
